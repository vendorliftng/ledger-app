# Copy this file to .dev.vars for local testing (wrangler pages dev).
# .dev.vars is gitignored — never commit your real Apps Script URL.
APPS_SCRIPT_URL=https://script.google.com/macros/s/PASTE_YOUR_DEPLOYMENT_ID/exec
